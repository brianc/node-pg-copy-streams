language: node_js
node_js:
  - '8'
  - '10'
  - '12'



addons:
  postgresql: '11.8'

services:
  - postgresql

before_install:
  - npm install npm --global

env:
  - PGVERSION=11.8 PGUSER=postgres PGDATABASE=postgres

before_script:
  - sudo bash -c "ls -al /etc/postgresql/"
